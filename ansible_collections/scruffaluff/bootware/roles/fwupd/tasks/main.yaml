---
- name: Install Fwupd for Linux
  ansible.builtin.package:
    name: fwupd
    state: present
  become: true
  tags:
    - install
  when:
    - system_install
    - ansible_facts.system == "Linux"

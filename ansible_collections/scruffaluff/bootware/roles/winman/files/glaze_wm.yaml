# GlazeWM configuration file for tiling windows.
#
# For more information, visit https://github.com/glazerdesktop/GlazeWM#configuration.

---
bar:
  enabled: false

binding_modes: []

focus_borders:
  active:
    enabled: false
  inactive:
    enabled: false

gaps:
  inner_gap: "2px"
  outer_gap: "4px"

general:
  window_animations: false

keybindings:
  # Kill GlazeWM process safely.
  - binding: "Control+Shift+LWin+E"
    command: "exit wm"
  # Re-evaluate configuration file.
  - binding: "Control+Shift+LWin+R"
    command: "reload config"
  # Change the focused window to be floating or tiling.
  - binding: "Shift+LWin+F"
    command: "toggle floating"
  # Change focused window to be maximized or unmaximized.
  - binding: "Shift+LWin+M"
    command: "toggle maximized"
  # Shift focus in a given direction.
  - bindings: ["Shift+LWin+J"]
    command: "focus left"
  - bindings: ["Shift+LWin+OemSemicolon"]
    command: "focus right"
  - bindings: ["Shift+LWin+L"]
    command: "focus up"
  - bindings: ["Shift+LWin+K"]
    command: "focus down"
  # Move focused window in a given direction.
  - bindings: ["Control+Shift+LWin+J"]
    command: "move left"
  - bindings: ["Control+Shift+LWin+OemSemicolon"]
    command: "move right"
  - bindings: ["Control+Shift+LWin+L"]
    command: "move up"
  - bindings: ["Control+Shift+LWin+K"]
    command: "move down"

window_rules:
  # Task Manager requires admin privileges to manage and should be ignored.
  - command: "ignore"
    match_process_name: "/Taskmgr|ScreenClippingHost/"
  # Launches system dialogs as floating windows.
  - command: "set floating"
    match_class_name: "#32770"

workspaces:
  - name: "1"
  - name: "2"
  - name: "3"
  - name: "4"
  - name: "5"

{
  "private": true,
  "scripts": {
    "bats:test": "bats -r tests/unit/",
    "prettier:format": "prettier --write .",
    "prettier:test": "prettier --check .",
    "shellcheck:test": "for file in $(find . -type f -name '.bash_profile' -o -name '.bashrc' -o -name '*.bash' -o -name '*.bats' -o -name '*.fish' -o -name '*.sh'); do shellcheck $file; done",
    "shfmt:format": "node scripts/shfmt.js format",
    "shfmt:test": "node scripts/shfmt.js test",
    "vuepress:build": "node scripts/build_docs.js",
    "vuepress:serve": "vuepress dev docs/"
  },
  "devDependencies": {
    "bats": "^1.2.1",
    "bats-assert": "https://github.com/ztombol/bats-assert",
    "bats-support": "https://github.com/ztombol/bats-support",
    "commander": "^8.1.0",
    "mustache": "^4.1.0",
    "prettier": "^2.0.5",
    "vuepress": "^1.5.3",
    "vuepress-plugin-code-copy": "^1.0.6"
  }
}

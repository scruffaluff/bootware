user_home_dir_linux: >-
  {{ '/root' if user_account == 'root' else '/home/' + user_account }}
user_home_dir: >-
  {{ user_home_dir_linux if ansible_system == 'Linux' else '/Users/' +
  user_account }}

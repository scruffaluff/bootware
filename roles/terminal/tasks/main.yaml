- name: Install Alacritty for Debian distributions
  apt:
    name: alacritty
    state: latest
    update_cache: yes
  become: yes
  when: ansible_os_family in ["Debian", "Pop!_OS", "Ubuntu"]

- name: Install Alacritty for MacOS
  become_user: "{{ user_account }}"
  community.general.homebrew_cask:
    name: alacritty
    state: present
  when: ansible_system == "Darwin"

- name: Create Alacritty configuration directory for Unix
  become_user: "{{ user_account }}"
  file:
    path: "{{ user_home_dir}}/.config/alacritty/"
    state: directory
  when: ansible_system in ["Darwin", "Linux"]

- name: Create Alacritty configuration directory for Windows
  become_user: "{{ user_account }}"
  file:
    path: "{{ user_home_dir}}/AppData/Roaming/alacritty/"
    state: directory
  when: ansible_system == "Windows"

- name: Copy Alacritty configuration file for Unix
  become_user: "{{ user_account }}"
  template:
    dest: "{{ user_home_dir}}/.config/alacritty/alacritty.yml"
    force: yes
    mode: 0644
    src: alacritty.j2
  when: ansible_system in ["Darwin", "Linux"]

- name: Copy Alacritty configuration file for Windows
  become_user: "{{ user_account }}"
  template:
    dest: "{{ user_home_dir}}/AppData/Roaming/alacritty/alacritty.yml"
    force: yes
    mode: 0644
    src: alacritty.j2
  when: ansible_system == "Windows"

- name: Tap Homebrew fonts for MacOS
  become_user: "{{ user_account }}"
  community.general.homebrew_tap:
    name: homebrew/cask-fonts
  when: ansible_system == "Darwin"

- name: Change GNOME terminal default profile for Linux
  become_user: "{{ user_account }}"
  shell:
    cmd: |
      GNOME_TERMINAL_PROFILE=$(gsettings get org.gnome.Terminal.ProfilesList default)
      gsettings set org.gnome.Terminal.Legacy.Profile:/org/gnome/legacy/profiles:/:$GNOME_TERMINAL_PROFILE/ cursor-background-color "rgb(0,0,0)"
      gsettings set org.gnome.Terminal.Legacy.Profile:/org/gnome/legacy/profiles:/:$GNOME_TERMINAL_PROFILE/ cursor-blink-mode on
      gsettings set org.gnome.Terminal.Legacy.Profile:/org/gnome/legacy/profiles:/:$GNOME_TERMINAL_PROFILE/ cursor-shape ibeam
      gsettings set org.gnome.Terminal.Legacy.Profile:/org/gnome/legacy/profiles:/:$GNOME_TERMINAL_PROFILE/ font "Fira Code 14"
      gsettings set org.gnome.Terminal.Legacy.Profile:/org/gnome/legacy/profiles:/:$GNOME_TERMINAL_PROFILE/ visible-name "Solarized Light"
  when: ansible_system == "Linux"

- name: Install Fira Code font for Debian distributions
  apt:
    name: fonts-firacode
    state: latest
    update_cache: yes
  become: yes
  when: ansible_os_family in ["Debian", "Pop!_OS", "Ubuntu"]

- name: Install Fira Code font for MacOS
  become_user: "{{ user_account }}"
  community.general.homebrew_cask:
    name: font-fira-code
    state: present
  when: ansible_system == "Darwin"

- name: Download Hyper for Debian distributions
  get_url:
    dest: /tmp/hyper.deb
    mode: 0700
    url: https://releases.hyper.is/download/deb
  when: ansible_os_family in ["Debian", "Pop!_OS", "Ubuntu"]

- name: Install Hyper for Debian distributions
  apt:
    deb: /tmp/hyper.deb
  become: yes
  when: ansible_os_family in ["Debian", "Pop!_OS", "Ubuntu"]

- name: Install Hyper for MacOS
  become_user: "{{ user_account }}"
  community.general.homebrew_cask:
    name: hyper
    state: present
  when: ansible_system == "Darwin"

- name: Create Hyper configuration directory for Linux
  become_user: "{{ user_account }}"
  file:
    path: "{{ user_home_dir}}/.config/Hyper/"
    state: directory
  when: ansible_system == "Linux"

- name: Create Hyper configuration directory for MacOS
  become_user: "{{ user_account }}"
  file:
    path: "{{ user_home_dir}}/Library/Application Support/Hyper/"
    state: directory
  when: ansible_system == "Darwin"

- name: Create Hyper configuration directory for Windows
  become_user: "{{ user_account }}"
  file:
    path: "{{ user_home_dir}}/AppData/Roaming/Hyper/"
    state: directory
  when: ansible_system == "Windows"

- name: Copy Hyper configuration file for Linux
  become_user: "{{ user_account }}"
  template:
    dest: "{{ user_home_dir}}/.config/Hyper/.hyper.js"
    force: yes
    mode: 0644
    src: hyper.j2
  when: ansible_system == "Linux"

- name: Copy Hyper configuration file for MacOS
  become_user: "{{ user_account }}"
  template:
    dest: "{{ user_home_dir}}/Library/Application Support/Hyper/.hyper.js"
    force: yes
    mode: 0644
    src: hyper.j2
  when: ansible_system == "Darwin"

- name: Copy Hyper configuration file for Windows
  become_user: "{{ user_account }}"
  template:
    dest: "{{ user_home_dir}}/AppData/Roaming/Hyper/.hyper.js"
    force: yes
    mode: 0644
    src: hyper.j2
  when: ansible_system == "Windows"

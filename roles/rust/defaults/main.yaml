cargo_applications:
  always:
    - cargo-binutils
    - cargo-edit
    # Cargo Generate currently has compilation issues which break installations.
    # - cargo-generate
    - cargo-make
    - cargo-watch
  linux:
    - cargo-tarpaulin
rustup_components:
  - clippy
  - llvm-tools-preview
  - rust-src
  - rustfmt
rustup_targets:
  - wasm32-wasi

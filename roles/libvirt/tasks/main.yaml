- name: Install Libvirt for Alpine distributions
  community.general.apk:
    name: libvirt-client
  when: ansible_pkg_mgr == "apk"

- name: Install Libvirt for Arch distributions
  community.general.pacman:
    name:
      - libvirt
      - virt-install
      - virt-viewer
  when: ansible_pkg_mgr == "pacman"

- name: Install Libvirt for Debian distributions
  apt:
    name:
      - libvirt-clients
      - libvirt-daemon-system
      - virt-viewer
      - virtinst
  when: ansible_pkg_mgr == "apt"

- name: Install Libvirt for FreeBSD
  community.general.pkgng:
    name: libvirt
  when: ansible_system == "FreeBSD"

- name: Install Libvirt for Fedora distributions
  dnf:
    name:
      - libvirt
      - virt-viewer
  when: ansible_pkg_mgr == "dnf"

- name: Tap Virt Manager Homebrew repository
  community.general.homebrew_tap:
    name: jeffreywildman/homebrew-virt-manager
  when: ansible_system == "Darwin"

- name: Install Libvirt for MacOS
  become: true
  become_user: "{{ user_account }}"
  community.general.homebrew:
    name:
      - libvirt
      # - jeffreywildman/homebrew-virt-manager/virt-manager
      - jeffreywildman/homebrew-virt-manager/virt-viewer
  when: ansible_system == "Darwin"
# Scoop appears to have dropped support for Libvirt.
# - name: Install Libvirt for Windows
#   become: true
#   become_user: "{{ user_account }}"
#   community.windows.win_scoop:
#     architecture: 64bit
#     name: libvirt
#   when: ansible_system == "Win32NT"

# - name: Install Virt Viewer for Windows
#   when: ansible_system == "Win32NT"
#   win_chocolatey:
#     name: virt-viewer
